# DeepSeek Harness 学习笔记

日期：2026-09-13
环境：Windows，Node `v22.22.2`，pnpm `11.7.0`
进度：**第 7 课讲完，第 7 课作业还没做。** 下次从作业开始。

这不是官方文档，是我自己跟下来的课。有些词当时听不懂，后来用自己的话记下了。

---

## 我现在卡在哪

- 第 1～6 课：做过 / 对过答案。
- 第 7 课：内容听过了，作业三问还没写。
- 第 8 课还没选：A 界面 / B 改仓库里的真文件 / C 把 greet 做成可配置问候语。

练习代码在本目录：`scratch-plugin/`。不要把 greet 写进产品默认清单。

---

## 第 0 课：先用人话记住它是什么

DeepSeek Harness（仓库里叫 dsh）不是又一个前端框架。更像「给 AI 用的应用壳」。

三句话就够：

1. 我在网页里打字，程序把话发给 DeepSeek。
2. 模型可以回文字，也可以说「请执行某某工具」；程序真的去跑，再把结果喂回给模型。
3. 仓库里几乎每块功能都是插件。启动时把很多插件拼在一起，就变成完整产品。

`ctx`：插件拿到的工具箱。要登记工具、听事件，都通过它，而不是满仓库去 import 别人内部实现。

以后在这里开发，大概就三类事：

1. 加小插件（给 AI 多一个工具）
2. 改已有功能
3. 改网页（`packages/client/`）

新手从第 1 类开始。Session、profile、seam 这些词当时先放下了。

---

## 第 1 课：把电脑配到能开发

仓库已经在本地：`D:/QQQQQ/ClaudeCode/9/deepseek-harness`。

做过的命令：

```powershell
node -v              # v22.22.2  （要 22.19+ 或 24+）
pnpm --version       # 11.7.0
pnpm install
pnpm run typecheck   # 通过，没有 error
```

`pnpm install` 会警告 `darwin-*` / `linux-*` 的 native 包：当前是 win32/x64，那些是给 Mac/Linux 的，跳过就行，不是失败。输出里有 `Already up to date` / `Done in ... using pnpm v11.7.0`。

官方说 typecheck 过了就算环境搭好。完整 `pnpm run build` 当时没强制；后来启动网页才发现需要先 build 一次。

密钥：老师提过根目录 `.env` 给测试和无界面命令用。我这边网页启动后会让填 API Key，填完就能聊。仓库里本来就没有 `.env`（被 git 忽略）。网页里填的密钥在本机 Harness 用户设置里，不在仓库文件里。对我来说这样就可以。

---

## 第 2 课：第一个插件 —— 启动时打一行日志

插件 = 一个 TS 文件，导出 `apply`。程序启动时调用它，并传入 `ctx`。

建了：

- `scratch-plugin/src/my-plugin.ts`
- `scratch-plugin/cordis.yml`

yml 必须是**绝对路径**，Windows 用正斜杠也可以：

```yaml
- insert:
    - id: hello
      name: 'D:/QQQQQ/ClaudeCode/9/deepseek-harness/scratch-plugin/src/my-plugin.ts'
```

人话：往正在跑的程序里多插一个插件。`--patch` 就是叠这张配置。

第一次 `pnpm dsh web --patch ...` 报错，**`pnpm build` 一次之后就能启动了。**
`dsh web` 会用编译产物；新环境或很久没编过会缺文件。编过一次后，改练习插件一般不用每次 build。

终端里看到了：

```text
[hello-plugin] plugin loaded!
```

地址一般是 `http://127.0.0.1:3080`。停服务：那个终端 `Ctrl+C`。

（当时终端里命令被折行，路径好像显示成 `scratch-pllugin`，以实际能加载为准。我的文件在 `scratch-plugin/`。）

---

## 第 3 课：给 AI 加工具 greet

把 `my-plugin.ts` 换成登记一个真正的工具。yml 不用改，还是指向同一个文件。

现在的代码大意：

- `inject = ['tools']`：先等「工具登记处」准备好再加载
- `ctx.tools.register(defineTool({...}))`：挂上工具
- `name` / `description` / `parameters`：给模型看的说明书
- `execute`：真干活，返回 `` Hello, ${args.name}! ``
- `output.render`：把返回值变成模型能读的文本

重启网页后对模型说：

> Use the greet tool to greet Ada.

结果：`Done — the greet tool returned: Hello, Ada!`
说明：插件挂上了，说明书被看见了，`execute` 也跑了。

---

## 第 4 课：点发送之后发生了什么

最短地图：

```text
我打字发送
  → 程序记下「用户说了这句话」
  → 把说明书（包括 greet）+ 历史打包，发给 DeepSeek
  → 模型说：请调用 greet，参数 name=Ada
  → 程序跑我写的 execute，得到 Hello, Ada!
  → 把这句话再发给模型
  → 模型用正常人话回复我
```

一轮「模型请求 + 它点名的工具」可以叫一次**步骤**；我点一次发送直到模型不再调工具，可以叫一次**回合**。名字先混着用也没关系，地图更重要。

和前端的对照：点提交 ≈ 发送；POST 后端 ≈ 发给 DeepSeek 并附带工具列表；后端 action ≈ 模型选择 greet；业务函数 ≈ `execute`；再请求拿最终文案 ≈ 模型看到工具结果后回复。差别是：可调用列表来自各插件的 `register`，不是我写死的一份 API 文档。

不要混的三份东西：

1. **说明书**（name / description / parameters）—— 模型靠这个决定调不调
2. **execute** —— 模型进不来，只能点名 + 填参数
3. **网页气泡** —— 界面画出来的；刷新能还原，是因为在重放一份只能往后追加的记录（以后会叫 Session 日志），不是 React 自己偷偷存的那份

`scratch-plugin` 只插入了「登记处多一个 greet」。循环、网页、存档都是仓库里别的插件做的。口诀：**先加工具，不要先改引擎。**

### 第 4 课作业（已做）

1. 模型从来没调用 greet → 更可能是说明书有问题（或插件没挂上）。因为还没走到 execute。
   后来补了一句：yml 路径错、忘了 `--patch`、忘了 `inject = ['tools']`，同样会「从不调用」。没调用时先问插件有没有挂上，再问说明书清不清楚。
2. 调用了但返回错字（Helo）→ 改 execute。

---

## 第 5 课：在仓库里找该改的文件

不是一个大 `src/`，而是 `packages/` 里很多小包，每个干一件事。打开文件前先问：改的是模型能力、网页，还是对话怎么存？

| 路径 | 是什么 | 我要不要乱进 |
|---|---|---|
| `scratch-plugin/` | 练习场 | 随便改 |
| `packages/` | 产品代码 | 要改功能才进 |
| `docs/` | 说明 | 迷路时查 |
| `apps/` | 启动入口 | 很少改 |
| `vendor/` | 拷进来的 Cordis 等 | 不要改 |

`packages/` 里大概：

- `core/`：循环、工具登记处、系统提示（引擎）
- `fs/`、`shell/`、`web/`：读文件、跑命令、上网
- `client/`：网页
- `llm/`：对接模型

安全区：练习插件；已有工具包里改文案 / 说明书 / execute 小逻辑；以后改 `packages/client/`。
谨慎区：很多包装依赖的接口包；配置、权限、沙箱。
危险区：`packages/core/agent-loop/`；`vendor/`；Session 日志格式/版本。

找文件用搜索，比逛文件夹快。例如 `name: 'todo_write'`，范围 `packages/todo`。

### 第 5 课作业（已做）

找到了：`packages/todo/tool-todo/src/index.ts`
搜索：`name: 'todo_write'`，范围 `packages/todo`。
它在更新 todo list（当前这次对话的任务清单）：每次交上一份完整列表，增、改状态、勾掉。和 greet 同类：上面说明书，下面 execute，只是更长（校验、写入会话记录）。

旁边还有 `tests/tool-todo.spec.ts` 等，当时打开过，第 7 课作业才要求扫测试标题。

---

## 第 6 课：练习插件 vs 正规包

会写 greet，就等于会写产品工具的心脏。正规包多的是：让仓库承认它、测它、写进默认启动。

`packages/todo/tool-todo/` 大概有：

```text
src/index.ts      心脏
src/types.ts      类型
tests/            自动检查
package.json      身份证，名字 @deepseek-ai/dsh-tool-todo
README.md
tsconfig.json
```

### 第 6 课作业：我找的 vs 老师纠正的

我找到了 `packages/bundle/base/package.json` 第 108 行：

```text
"@deepseek-ai/dsh-tool-todo": "workspace:^"
```

当时理解：配一个工作区依赖。greet 之所以要 `--patch`，是因为没写进这份 package.json。

**纠正：只对了一半。**

- `package.json`：npm 购物清单，允许引用这个包。`workspace:^` = 用本仓库里的那份。
- **真正启动时挂上插件的是** `packages/bundle/base/cordis.patch.yml` 大约 401–404 行：

```yaml
- id: tool-todo
  name: '@deepseek-ai/dsh-tool-todo'
  config:
    allowParallelInProgress: true
```

这和我的 `scratch-plugin/cordis.yml` 是同一类东西：一边是 `id: hello` + 磁盘绝对路径 + 启动时 `--patch`；一边是 `id: tool-todo` + npm 包名 + 写进 base，每次启动都有。

只有依赖、没有挂载，等于买了包也不会加载。greet 需要 `--patch`，是因为 **yml 清单里没有那一行**，不是因为没写进 package.json。两份都要：能引用 + 写进启动清单。

不要把 greet 写进产品的 `cordis.patch.yml`，那会变成所有人默认都有的玩具。

---

## 第 7 课：接到「加功能」怎么动手（内容已听，作业未做）

顺序：

1. 给模型用的，还是给人看的？模型能点名 → 工具；只是网页 → `packages/client/`；都要 → 先工具再界面。
2. 先在 `scratch-plugin` + `--patch` 验证。错了删练习文件就行。
3. 只有「希望别人启动也自带」才进 `packages/`（正规包、测试、依赖 + 挂载两份清单）。
4. 正规包改行为，优先跑这个包的 `tests/`，不要默认跑全仓库。练习场用网页 + 让模型调用。

```text
新想法
  → 练习插件做出来
  → 网页里真的能调
  → 要成为默认功能？
        否 → 停在 --patch
        是 → 再搬进 packages/
```

### 第 7 课作业（未做）

打开 `packages/todo/tool-todo/tests/tool-todo.spec.ts`，扫 `it(` / `test(` 标题，然后回答：

1. `package.json` 和 `cordis.patch.yml` 各负责哪一步？
2. 再做一个只有自己开发时用的、类似 greet 的工具，会不会改 `packages/bundle/base`？为什么？
3. 测试文件标题大概在检查什么？（一句话）

做完可以选第 8 课：A 界面 / B 改一个很小的真文件并跑测试 / C 可配置问候语。

---

## 我自己的命令备忘

```powershell
# 环境
pnpm install
pnpm run typecheck
pnpm run build          # 第一次起 web 之前需要；之后改练习插件通常不用

# 带着练习插件起网页
pnpm dsh web --patch ./scratch-plugin/cordis.yml
```

网页填 API Key，选工作区，才能在输入框里聊天。

---

## 先别碰

- `packages/core/agent-loop/`
- `vendor/`
- Session 日志格式 / 版本
- 把练习工具写进 `packages/bundle/base/cordis.patch.yml`

---

## 对照官方教程（迷路再点）

练习路径对应仓库里的用户教程，不是我编的：

- 第一个插件：`docs/user/develop/basic/index.zh.md`
- 开发一个工具：`docs/user/develop/basic/tool.zh.md`
- 下一小步（可配置，第 8 课 C）：`docs/user/develop/basic/config.zh.md`
- 开发指南：`docs/development.zh.md`
- 架构（现在还早）：`docs/architecture.zh.md`
